import random

def threes(x):
	while x!=1:
		if x%3==0:
			z=x/3
			print str(x)+" "+str(0)
			x=z
		elif x%3!=0:
			d=random.randint(0,1)
			if d==0:
				d=-1
			z=x+d
			print str(x)+" "+str(d)
			x=z
	print x

threes(31337357)
